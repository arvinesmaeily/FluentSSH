<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FluentSSH.Pages.ConfigurationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentSSH.Pages"
    xmlns:models="using:FluentSSH.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer>
        <StackPanel Spacing="4" Margin="0,0,0,24">
            
            <!-- Add Configuration Card -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Add a new configuration" 
                               VerticalAlignment="Center"
                               Style="{StaticResource BodyTextBlockStyle}"/>
                    
                    <!-- Add Button - Green -->
                    <Button Grid.Column="1"
                            Click="AddConfigurationButton_Click"
                            Style="{StaticResource GreenButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE710;" FontSize="14"/>
                            <TextBlock Text="Add configuration"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Configurations List -->
            <ItemsControl x:Name="ConfigurationsList" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:SSHConfiguration">
                        <Border x:Name="ConfigBorder"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,4"
                            PointerPressed="ConfigItem_PointerPressed"
                            PointerEntered="ConfigItem_PointerEntered"
                            PointerExited="ConfigItem_PointerExited"
                            Loaded="ConfigItem_Loaded">
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Selection Indicator Bar -->
                                <Border x:Name="SelectionIndicator"
                                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                                        CornerRadius="2,0,0,2"
                                        Visibility="Collapsed"/>
                                
                                <!-- Server Icon -->
                                <FontIcon Grid.Column="1"
                                          Glyph="&#xE839;" 
                                          FontSize="24"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="12,16,16,16"/>
                                
                                <!-- Config Info -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,16">
                                    <TextBlock Text="{x:Bind Name}" 
                                               Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                        <Run Text="{x:Bind ServerAddress}"/>
                                        <Run Text=":"/>
                                        <Run Text="{x:Bind ServerPort}"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <!-- Edit Button -->
                                <Button Grid.Column="3"
                                        Tag="{x:Bind}"
                                        Click="EditConfigButton_Click"
                                        Margin="8,0">
                                    <TextBlock Text="Edit"/>
                                </Button>
                                
                                <!-- Remove Button - Red -->
                                <Button Grid.Column="4"
                                        Tag="{x:Bind}"
                                        Click="RemoveConfigButton_Click"
                                        Margin="0,0,16,0"
                                        Style="{StaticResource RedButtonStyle}">
                                    <TextBlock Text="Remove"/>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Empty State -->
            <Border x:Name="EmptyState"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="32"
                    Margin="0,8,0,0"
                    Visibility="Collapsed">
                
                <StackPanel HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE774;" 
                              FontSize="48"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="No configurations" 
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,4"/>
                    <TextBlock Text="Add a configuration to get started"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
